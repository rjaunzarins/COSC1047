package Assignment2;
/*
public class Vampire extends Monster {
	
	private static int maxHealth = 12;
	private static int minHealth = 8;
	
	private static int maxDamage = 9;
	private static int minDamage = 6;
	
	public Vampire() {
		super( (int)Math.random() * (maxHealth - minHealth + 1 ) + minHealth);
	}
	
	@Override
	public int dealDamage() {
		super( (int)Math.random() * (maxDamage - minDamage + 1 ) + minDamage);
	}
	
	@Override
	public String toString() {
		return "Vampire Health: " + getHealth();
	}
}

// Must make Monster Class for this code to work

*/